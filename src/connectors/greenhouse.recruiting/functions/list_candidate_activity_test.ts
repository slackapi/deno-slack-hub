/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import ListCandidateActivity from "./list_candidate_activity.ts";

Deno.test("ListCandidateActivity can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_ListCandidateActivity_slack_function",
    title: "Test ListCandidateActivity",
    description: "This is a generated test to test ListCandidateActivity",
  });
  testWorkflow.addStep(ListCandidateActivity, { candidate_id: "test" });
  const actual = testWorkflow.steps[0].export();

  assertEquals(
    actual.function_id,
    "A05QS03T4MB#/functions/list_candidate_activity",
  );
  assertEquals(actual.inputs, { candidate_id: "test" });
});

Deno.test("All outputs of Slack function ListCandidateActivity should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_ListCandidateActivity_slack_function",
    title: "Test ListCandidateActivity",
    description: "This is a generated test to test ListCandidateActivity",
  });
  const step = testWorkflow.addStep(ListCandidateActivity, {
    candidate_id: "test",
  });
  assertExists(step.outputs.notes);
  assertExists(step.outputs.activities);
});
