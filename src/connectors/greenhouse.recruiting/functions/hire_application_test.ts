/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import HireApplication from "./hire_application.ts";

Deno.test("HireApplication can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_HireApplication_slack_function",
    title: "Test HireApplication",
    description: "This is a generated test to test HireApplication",
  });
  testWorkflow.addStep(HireApplication, {
    candidate_id: "test",
    application_id: "test",
    close_reason_id: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05QS03T4MB#/functions/hire_application");
  assertEquals(actual.inputs, {
    candidate_id: "test",
    application_id: "test",
    close_reason_id: "test",
  });
});

Deno.test("All outputs of Slack function HireApplication should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_HireApplication_slack_function",
    title: "Test HireApplication",
    description: "This is a generated test to test HireApplication",
  });
  const step = testWorkflow.addStep(HireApplication, {
    candidate_id: "test",
    application_id: "test",
    close_reason_id: "test",
  });
  assertExists(step.outputs.id);
  assertExists(step.outputs.status);
  assertExists(step.outputs.candidate_id);
});
