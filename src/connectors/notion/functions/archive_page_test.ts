/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import ArchivePage from "./archive_page.ts";

Deno.test("ArchivePage can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_ArchivePage_slack_function",
    title: "Test ArchivePage",
    description: "This is a generated test to test ArchivePage",
  });
  testWorkflow.addStep(ArchivePage, { page_id: "test" });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05SUU730P2#/functions/archive_page");
  assertEquals(actual.inputs, { page_id: "test" });
});

Deno.test("All outputs of Slack function ArchivePage should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_ArchivePage_slack_function",
    title: "Test ArchivePage",
    description: "This is a generated test to test ArchivePage",
  });
  const step = testWorkflow.addStep(ArchivePage, { page_id: "test" });
  assertExists(step.outputs.page_id);
  assertExists(step.outputs.page_url);
});
