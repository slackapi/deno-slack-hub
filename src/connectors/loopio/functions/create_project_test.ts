/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CreateProject from "./create_project.ts";

Deno.test("CreateProject can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateProject_slack_function",
    title: "Test CreateProject",
    description: "This is a generated test to test CreateProject",
  });
  testWorkflow.addStep(CreateProject, {
    project_name: "test",
    company_name: "test",
    due_date: "test",
    project_type: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05UAJ4398T#/functions/create_project");
  assertEquals(actual.inputs, {
    project_name: "test",
    company_name: "test",
    due_date: "test",
    project_type: "test",
  });
});

Deno.test("All outputs of Slack function CreateProject should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateProject_slack_function",
    title: "Test CreateProject",
    description: "This is a generated test to test CreateProject",
  });
  const step = testWorkflow.addStep(CreateProject, {
    project_name: "test",
    company_name: "test",
    due_date: "test",
    project_type: "test",
  });
  assertExists(step.outputs.project_id);
  assertExists(step.outputs.project_name);
  assertExists(step.outputs.description);
  assertExists(step.outputs.created_date);
  assertExists(step.outputs.due_date);
  assertExists(step.outputs.status);
  assertExists(step.outputs.company_name);
  assertExists(step.outputs.owner);
  assertExists(step.outputs.creator);
});
