/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import UpdateMeeting from "./update_meeting.ts";

Deno.test("UpdateMeeting can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateMeeting_slack_function",
    title: "Test UpdateMeeting",
    description: "This is a generated test to test UpdateMeeting",
  });
  testWorkflow.addStep(UpdateMeeting, {
    teams_access_token: "test",
    meeting_id: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05Q9UPHBDL#/functions/update_meeting");
  assertEquals(actual.inputs, {
    teams_access_token: "test",
    meeting_id: "test",
  });
});

Deno.test("All outputs of Slack function UpdateMeeting should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateMeeting_slack_function",
    title: "Test UpdateMeeting",
    description: "This is a generated test to test UpdateMeeting",
  });
  const step = testWorkflow.addStep(UpdateMeeting, {
    teams_access_token: "test",
    meeting_id: "test",
  });
  assertExists(step.outputs.meeting_url);
  assertExists(step.outputs.meeting_id);
});
