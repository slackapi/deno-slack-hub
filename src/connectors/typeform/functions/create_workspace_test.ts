/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CreateWorkspace from "./create_workspace.ts";

Deno.test("CreateWorkspace can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateWorkspace_slack_function",
    title: "Test CreateWorkspace",
    description: "This is a generated test to test CreateWorkspace",
  });
  testWorkflow.addStep(CreateWorkspace, {
    workspace_name: "test",
    typeform_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05T70HT5E1#/functions/create_workspace");
  assertEquals(actual.inputs, {
    workspace_name: "test",
    typeform_access_token: "test",
  });
});

Deno.test("All outputs of Slack function CreateWorkspace should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateWorkspace_slack_function",
    title: "Test CreateWorkspace",
    description: "This is a generated test to test CreateWorkspace",
  });
  const step = testWorkflow.addStep(CreateWorkspace, {
    workspace_name: "test",
    typeform_access_token: "test",
  });
  assertExists(step.outputs.workspace_url);
  assertExists(step.outputs.workspace_id);
  assertExists(step.outputs.workspace_name);
});
