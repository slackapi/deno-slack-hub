/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import UpdateEvent from "./update_event.ts";

Deno.test("UpdateEvent can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateEvent_slack_function",
    title: "Test UpdateEvent",
    description: "This is a generated test to test UpdateEvent",
  });
  testWorkflow.addStep(UpdateEvent, {
    calendar_id: "test",
    event_id: "test",
    start_time: "test",
    end_time: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A050C90PUF5#/functions/update_event");
  assertEquals(actual.inputs, {
    calendar_id: "test",
    event_id: "test",
    start_time: "test",
    end_time: "test",
  });
});

Deno.test("All outputs of Slack function UpdateEvent should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateEvent_slack_function",
    title: "Test UpdateEvent",
    description: "This is a generated test to test UpdateEvent",
  });
  const step = testWorkflow.addStep(UpdateEvent, {
    calendar_id: "test",
    event_id: "test",
    start_time: "test",
    end_time: "test",
  });
  assertExists(step.outputs.event_summary);
  assertExists(step.outputs.event_url);
});
