/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CancelBuild from "./cancel_build.ts";

Deno.test("CancelBuild can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CancelBuild_slack_function",
    title: "Test CancelBuild",
    description: "This is a generated test to test CancelBuild",
  });
  testWorkflow.addStep(CancelBuild, {
    repo_slug: "test",
    branch: "test",
    build_id: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A0619K2GN2W#/functions/cancel_build");
  assertEquals(actual.inputs, {
    repo_slug: "test",
    branch: "test",
    build_id: "test",
  });
});

Deno.test("All outputs of Slack function CancelBuild should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CancelBuild_slack_function",
    title: "Test CancelBuild",
    description: "This is a generated test to test CancelBuild",
  });
  const step = testWorkflow.addStep(CancelBuild, {
    repo_slug: "test",
    branch: "test",
    build_id: "test",
  });
  assertExists(step.outputs.build_id);
  assertExists(step.outputs.branch);
  assertExists(step.outputs.repository);
  assertExists(step.outputs.build_url);
});
