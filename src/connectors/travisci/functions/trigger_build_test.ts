/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import TriggerBuild from "./trigger_build.ts";

Deno.test("TriggerBuild can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_TriggerBuild_slack_function",
    title: "Test TriggerBuild",
    description: "This is a generated test to test TriggerBuild",
  });
  testWorkflow.addStep(TriggerBuild, {
    repo_slug: "test",
    branch: "test",
    message: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A0619K2GN2W#/functions/trigger_build");
  assertEquals(actual.inputs, {
    repo_slug: "test",
    branch: "test",
    message: "test",
  });
});

Deno.test("All outputs of Slack function TriggerBuild should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_TriggerBuild_slack_function",
    title: "Test TriggerBuild",
    description: "This is a generated test to test TriggerBuild",
  });
  const step = testWorkflow.addStep(TriggerBuild, {
    repo_slug: "test",
    branch: "test",
    message: "test",
  });
  assertExists(step.outputs.request_id);
  assertExists(step.outputs.request_message);
  assertExists(step.outputs.branch);
  assertExists(step.outputs.repository);
});
