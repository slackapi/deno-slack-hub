/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import SendCampaign from "./send_campaign.ts";

Deno.test("SendCampaign can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_SendCampaign_slack_function",
    title: "Test SendCampaign",
    description: "This is a generated test to test SendCampaign",
  });
  testWorkflow.addStep(SendCampaign, {
    mailchimp_access_token: "test",
    campaign_id: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05TPV44YKG#/functions/send_campaign");
  assertEquals(actual.inputs, {
    mailchimp_access_token: "test",
    campaign_id: "test",
  });
});

Deno.test("All outputs of Slack function SendCampaign should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_SendCampaign_slack_function",
    title: "Test SendCampaign",
    description: "This is a generated test to test SendCampaign",
  });
  const step = testWorkflow.addStep(SendCampaign, {
    mailchimp_access_token: "test",
    campaign_id: "test",
  });
  assertExists(step.outputs.response);
});
