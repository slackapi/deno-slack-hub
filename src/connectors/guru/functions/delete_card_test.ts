/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import DeleteCard from "./delete_card.ts";

Deno.test("DeleteCard can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_DeleteCard_slack_function",
    title: "Test DeleteCard",
    description: "This is a generated test to test DeleteCard",
  });
  testWorkflow.addStep(DeleteCard, {
    card_id: "test",
    guru_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05RT1HSRRU#/functions/delete_card");
  assertEquals(actual.inputs, { card_id: "test", guru_access_token: "test" });
});

Deno.test("All outputs of Slack function DeleteCard should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_DeleteCard_slack_function",
    title: "Test DeleteCard",
    description: "This is a generated test to test DeleteCard",
  });
  const step = testWorkflow.addStep(DeleteCard, {
    card_id: "test",
    guru_access_token: "test",
  });
  assertExists(step.outputs.card_id);
});
