/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import UpdateCard from "./update_card.ts";

Deno.test("UpdateCard can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateCard_slack_function",
    title: "Test UpdateCard",
    description: "This is a generated test to test UpdateCard",
  });
  testWorkflow.addStep(UpdateCard, {
    card_id: "test",
    share_status: "test",
    guru_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05RT1HSRRU#/functions/update_card");
  assertEquals(actual.inputs, {
    card_id: "test",
    share_status: "test",
    guru_access_token: "test",
  });
});

Deno.test("All outputs of Slack function UpdateCard should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateCard_slack_function",
    title: "Test UpdateCard",
    description: "This is a generated test to test UpdateCard",
  });
  const step = testWorkflow.addStep(UpdateCard, {
    card_id: "test",
    share_status: "test",
    guru_access_token: "test",
  });
  assertExists(step.outputs.card_id);
  assertExists(step.outputs.card_title);
  assertExists(step.outputs.card_url);
  assertExists(step.outputs.verification_state);
});
