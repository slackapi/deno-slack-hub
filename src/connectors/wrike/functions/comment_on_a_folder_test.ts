/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CommentOnAFolder from "./comment_on_a_folder.ts";

Deno.test("CommentOnAFolder can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CommentOnAFolder_slack_function",
    title: "Test CommentOnAFolder",
    description: "This is a generated test to test CommentOnAFolder",
  });
  testWorkflow.addStep(CommentOnAFolder, {
    folder: "test",
    text: "test",
    wrike_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(
    actual.function_id,
    "A05MDPYMGJC#/functions/comment_on_a_folder",
  );
  assertEquals(actual.inputs, {
    folder: "test",
    text: "test",
    wrike_access_token: "test",
  });
});

Deno.test("All outputs of Slack function CommentOnAFolder should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CommentOnAFolder_slack_function",
    title: "Test CommentOnAFolder",
    description: "This is a generated test to test CommentOnAFolder",
  });
  const step = testWorkflow.addStep(CommentOnAFolder, {
    folder: "test",
    text: "test",
    wrike_access_token: "test",
  });
  assertExists(step.outputs.comment_id);
  assertExists(step.outputs.author_id);
  assertExists(step.outputs.text);
  assertExists(step.outputs.created_date);
  assertExists(step.outputs.folder_id);
});
