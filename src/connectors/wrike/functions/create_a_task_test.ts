/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CreateATask from "./create_a_task.ts";

Deno.test("CreateATask can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateATask_slack_function",
    title: "Test CreateATask",
    description: "This is a generated test to test CreateATask",
  });
  testWorkflow.addStep(CreateATask, {
    folder: "test",
    title: "test",
    wrike_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05MDPYMGJC#/functions/create_a_task");
  assertEquals(actual.inputs, {
    folder: "test",
    title: "test",
    wrike_access_token: "test",
  });
});

Deno.test("All outputs of Slack function CreateATask should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateATask_slack_function",
    title: "Test CreateATask",
    description: "This is a generated test to test CreateATask",
  });
  const step = testWorkflow.addStep(CreateATask, {
    folder: "test",
    title: "test",
    wrike_access_token: "test",
  });
  assertExists(step.outputs.task_id);
  assertExists(step.outputs.account_id);
  assertExists(step.outputs.title);
  assertExists(step.outputs.description);
  assertExists(step.outputs.importance);
});
