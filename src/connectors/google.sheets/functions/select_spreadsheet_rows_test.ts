/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import SelectSpreadsheetRows from "./select_spreadsheet_rows.ts";

Deno.test("SelectSpreadsheetRows can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_SelectSpreadsheetRows_slack_function",
    title: "Test SelectSpreadsheetRows",
    description: "This is a generated test to test SelectSpreadsheetRows",
  });
  testWorkflow.addStep(SelectSpreadsheetRows, {
    spreadsheet_id: "test",
    sheet_title: "test",
    google_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(
    actual.function_id,
    "A04MG80N5CY#/functions/select_spreadsheet_rows",
  );
  assertEquals(actual.inputs, {
    spreadsheet_id: "test",
    sheet_title: "test",
    google_access_token: "test",
  });
});

Deno.test("All outputs of Slack function SelectSpreadsheetRows should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_SelectSpreadsheetRows_slack_function",
    title: "Test SelectSpreadsheetRows",
    description: "This is a generated test to test SelectSpreadsheetRows",
  });
  const step = testWorkflow.addStep(SelectSpreadsheetRows, {
    spreadsheet_id: "test",
    sheet_title: "test",
    google_access_token: "test",
  });
  assertExists(step.outputs.row_items);
  assertExists(step.outputs.item_count);
  assertExists(step.outputs.columns);
  assertExists(step.outputs.spreadsheet_url);
  assertExists(step.outputs.timestamp_started);
  assertExists(step.outputs.timestamp_completed);
});
