/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CopyFile from "./copy_file.ts";

Deno.test("CopyFile can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CopyFile_slack_function",
    title: "Test CopyFile",
    description: "This is a generated test to test CopyFile",
  });
  testWorkflow.addStep(CopyFile, {
    onedrive_access_token: "test",
    file_id: "test",
    new_file_name: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05QGK4DF8A#/functions/copy_file");
  assertEquals(actual.inputs, {
    onedrive_access_token: "test",
    file_id: "test",
    new_file_name: "test",
  });
});

Deno.test("All outputs of Slack function CopyFile should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CopyFile_slack_function",
    title: "Test CopyFile",
    description: "This is a generated test to test CopyFile",
  });
  const step = testWorkflow.addStep(CopyFile, {
    onedrive_access_token: "test",
    file_id: "test",
    new_file_name: "test",
  });
  assertExists(step.outputs.response);
  assertExists(step.outputs.new_file_name);
  assertExists(step.outputs.file_url);
});
