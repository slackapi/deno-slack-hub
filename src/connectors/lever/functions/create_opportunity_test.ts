/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CreateOpportunity from "./create_opportunity.ts";

Deno.test("CreateOpportunity can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateOpportunity_slack_function",
    title: "Test CreateOpportunity",
    description: "This is a generated test to test CreateOpportunity",
  });
  testWorkflow.addStep(CreateOpportunity, { perform_as: "test", name: "test" });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A063ZSXC719#/functions/create_opportunity");
  assertEquals(actual.inputs, { perform_as: "test", name: "test" });
});

Deno.test("All outputs of Slack function CreateOpportunity should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateOpportunity_slack_function",
    title: "Test CreateOpportunity",
    description: "This is a generated test to test CreateOpportunity",
  });
  const step = testWorkflow.addStep(CreateOpportunity, {
    perform_as: "test",
    name: "test",
  });
  assertExists(step.outputs.opportunity_id);
  assertExists(step.outputs.opportunity_url);
});
