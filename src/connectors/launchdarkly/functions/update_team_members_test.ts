/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import UpdateTeamMembers from "./update_team_members.ts";

Deno.test("UpdateTeamMembers can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateTeamMembers_slack_function",
    title: "Test UpdateTeamMembers",
    description: "This is a generated test to test UpdateTeamMembers",
  });
  testWorkflow.addStep(UpdateTeamMembers, {
    team_key: "test",
    kind: "test",
    member_keys: "test",
    launchdarkly_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(
    actual.function_id,
    "A05RF94AFB3#/functions/update_team_members",
  );
  assertEquals(actual.inputs, {
    team_key: "test",
    kind: "test",
    member_keys: "test",
    launchdarkly_access_token: "test",
  });
});

Deno.test("All outputs of Slack function UpdateTeamMembers should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdateTeamMembers_slack_function",
    title: "Test UpdateTeamMembers",
    description: "This is a generated test to test UpdateTeamMembers",
  });
  const step = testWorkflow.addStep(UpdateTeamMembers, {
    team_key: "test",
    kind: "test",
    member_keys: "test",
    launchdarkly_access_token: "test",
  });
  assertExists(step.outputs.name);
  assertExists(step.outputs.key);
  assertExists(step.outputs.comment);
  assertExists(step.outputs.kind);
});
