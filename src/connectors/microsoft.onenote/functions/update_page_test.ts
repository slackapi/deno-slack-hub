/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import UpdatePage from "./update_page.ts";

Deno.test("UpdatePage can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdatePage_slack_function",
    title: "Test UpdatePage",
    description: "This is a generated test to test UpdatePage",
  });
  testWorkflow.addStep(UpdatePage, {
    page_id: "test",
    content: "test",
    microsoft_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05UBDPKYP8#/functions/update_page");
  assertEquals(actual.inputs, {
    page_id: "test",
    content: "test",
    microsoft_access_token: "test",
  });
});

Deno.test("All outputs of Slack function UpdatePage should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_UpdatePage_slack_function",
    title: "Test UpdatePage",
    description: "This is a generated test to test UpdatePage",
  });
  const step = testWorkflow.addStep(UpdatePage, {
    page_id: "test",
    content: "test",
    microsoft_access_token: "test",
  });
  assertExists(step.outputs.response_code);
});
