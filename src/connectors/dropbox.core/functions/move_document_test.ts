/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import MoveDocument from "./move_document.ts";

Deno.test("MoveDocument can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_MoveDocument_slack_function",
    title: "Test MoveDocument",
    description: "This is a generated test to test MoveDocument",
  });
  testWorkflow.addStep(MoveDocument, {
    document_type: "test",
    from_path: "test",
    name: "test",
    dropbox_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05U3EFESFR#/functions/move_document");
  assertEquals(actual.inputs, {
    document_type: "test",
    from_path: "test",
    name: "test",
    dropbox_access_token: "test",
  });
});

Deno.test("All outputs of Slack function MoveDocument should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_MoveDocument_slack_function",
    title: "Test MoveDocument",
    description: "This is a generated test to test MoveDocument",
  });
  const step = testWorkflow.addStep(MoveDocument, {
    document_type: "test",
    from_path: "test",
    name: "test",
    dropbox_access_token: "test",
  });
  assertExists(step.outputs.id);
  assertExists(step.outputs.name);
  assertExists(step.outputs.path_display);
});
