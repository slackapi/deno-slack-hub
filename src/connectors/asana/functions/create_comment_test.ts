/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CreateComment from "./create_comment.ts";

Deno.test("CreateComment can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateComment_slack_function",
    title: "Test CreateComment",
    description: "This is a generated test to test CreateComment",
  });
  testWorkflow.addStep(CreateComment, {
    workspace_gid: "test",
    task_gid: "test",
    comment_text: "test",
    asana_access_token: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05KUFPF86S#/functions/create_comment");
  assertEquals(actual.inputs, {
    workspace_gid: "test",
    task_gid: "test",
    comment_text: "test",
    asana_access_token: "test",
  });
});

Deno.test("All outputs of Slack function CreateComment should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateComment_slack_function",
    title: "Test CreateComment",
    description: "This is a generated test to test CreateComment",
  });
  const step = testWorkflow.addStep(CreateComment, {
    workspace_gid: "test",
    task_gid: "test",
    comment_text: "test",
    asana_access_token: "test",
  });
  assertExists(step.outputs.task_name);
  assertExists(step.outputs.comment_text);
});
