/** This file was autogenerated. Follow the steps in src/schema/slack/functions/README.md to rebuild **/
import { assertEquals, assertExists } from "../../../dev_deps.ts";
import { DefineWorkflow } from "../../../dev_deps.ts";
import CreateCandidate from "./create_candidate.ts";

Deno.test("CreateCandidate can be used as a Slack function in a workflow step", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateCandidate_slack_function",
    title: "Test CreateCandidate",
    description: "This is a generated test to test CreateCandidate",
  });
  testWorkflow.addStep(CreateCandidate, {
    first_name: "test",
    last_name: "test",
    email: "test",
  });
  const actual = testWorkflow.steps[0].export();

  assertEquals(actual.function_id, "A05UG19KZ3K#/functions/create_candidate");
  assertEquals(actual.inputs, {
    first_name: "test",
    last_name: "test",
    email: "test",
  });
});

Deno.test("All outputs of Slack function CreateCandidate should exist", () => {
  const testWorkflow = DefineWorkflow({
    callback_id: "test_CreateCandidate_slack_function",
    title: "Test CreateCandidate",
    description: "This is a generated test to test CreateCandidate",
  });
  const step = testWorkflow.addStep(CreateCandidate, {
    first_name: "test",
    last_name: "test",
    email: "test",
  });
  assertExists(step.outputs.candidate_id);
  assertExists(step.outputs.first_name);
  assertExists(step.outputs.last_name);
  assertExists(step.outputs.email);
  assertExists(step.outputs.phone_number);
});
